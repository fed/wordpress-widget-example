{
  "name": "wordpress-widget-example",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "custom-scripts": "^0.1.4",
    "npm-run-all": "^4.0.1"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "custom-scripts start",
    "build": "custom-scripts build",
    "test": "custom-scripts test",
    "postbuild": "npm-run-all --parallel widget:*",
    "widget:config": "cp wordpress-widget-example.php build/wordpress-widget-example.php",
    "widget:clean": "rm build/index.html build/favicon.ico"
  }
}
