<?php
/*
Plugin Name: {{name}}
Description: {{description}}
Version: {{version}}
Author: {{author.name}}
Author URI: {{author.url}}
*/

function wp_widget_setup() {
  echo '<div id="root"></div>';
}

function include_react_files() {
  wp_enqueue_style(
    'prefix-style',
    plugins_url('{{{css}}}', __FILE__)
  );

  wp_enqueue_script(
    'plugin-scripts',
    plugins_url('{{{js}}}', __FILE__),
    array(),
    '{{version}}',
    true
  );
}

add_action('wp_enqueue_scripts', 'include_react_files');
